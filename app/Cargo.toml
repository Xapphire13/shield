[package]
name = "shield-app"
version = "0.1.0"
authors = ["Xapphire13"]
edition = "2024"

[dependencies]
dioxus = { version = "0.6.1", features = [] }

[features]
default = ["web"]
web = ["dioxus/web"]
