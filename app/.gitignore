# Generated by Cargo
# will have compiled files and executables
/target
.DS_Store

# These are backup files generated by rustfmt
**/*.rs.bk

# Scoped component CSS bundled by the stylance CLI (run `scripts/dev.sh` or
# `stylance app`). Kept out of git so a missing/forgotten run fails the build
# loudly at `asset!()` instead of shipping stale styles.
/assets/styles.css
